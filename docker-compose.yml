services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - media-data:/app/media
    environment:
      - PYTHONDONTWRITEBYTECODE=1
      - PYTHONUNBUFFERED=1
      - ALLOWED_HOSTS=localhost,127.0.0.1

volumes:
  media-data:
